# Treat these as binary so git never applies text/EOL normalization (prevents
# corrupting embedded images in .pptx and other binaries across machines).
*.pptx binary
*.pdf  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.mat  binary
*.lfp  binary
*.dat  binary
